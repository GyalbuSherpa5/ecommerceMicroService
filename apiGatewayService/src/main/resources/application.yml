server:
  port: 8069

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/products/**

        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/users/**


